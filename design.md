# 轻盈金融后台设计规范

## 1. 设计目标

本规范定义一个用于资金账户、付款、转账和交易管理的桌面端后台界面。整体应传达**可信、清晰、现代且略带未来感**的气质：以大面积留白和卡片化信息保证金融数据的可读性，以柔和的蓝紫渐变和抽象 3D 丝带作为品牌记忆点。

- **适用场景**：银行/支付 SaaS、企业资金管理、财务运营后台。
- **优先级**：信息层级与可操作性优先于装饰；装饰图不应影响数据阅读或点击。
- **参考画布**：1440 × 980 左右的桌面端。侧栏固定，内容区最小宽度建议 1120px。
- **布局原则**：8px 基准栅格、圆角卡片、低对比边框、避免沉重阴影。

## 2. 页面信息架构

页面由固定左侧导航和右侧内容区组成：

1. **窄侧栏（约 72px）**：品牌标识、主导航图标、底部辅助入口与用户头像。
2. **账户概览 Hero**：返回入口、全局搜索、主操作按钮、余额和 KPI 指标。
3. **快捷操作**：Wire Transfer、ACH Transfer、Bill Payment、International 等 2 × 2 按钮。
4. **主内容双栏**：左栏为 Deposited Checks；右栏依次为 Spending Limit 和 Transactions。

在 1200px 以上使用两栏；在 1024–1199px 缩窄间距和卡片内边距；小于 1024px 时将主内容改为单栏，Hero KPI 可横向滚动或分两行显示。该设计主要面向桌面端，不要在宽屏上将内容拉得过散。

## 3. 版式与空间

### 3.1 外框

- 页面底色：`#F7F7FA` 至 `#FFFFFF` 的极浅中性色。
- 左侧栏：白色，右侧一条 `#ECECF2` 的 1px 分隔线。
- 内容容器：白色，大圆角 `0 0 24px 0`（或完整 24px，视是否贴合侧栏而定），溢出隐藏。
- 内容区内边距：桌面端 40–56px；区块垂直间距 28–40px。
- 常规卡片：白色背景、`1px solid #E5E5EC`、圆角 20–24px；只使用非常轻的阴影 `0 6px 18px rgba(31, 30, 56, .04)`，或不使用阴影。

### 3.2 栅格

- 采用 12 列内容栅格，列间距 20–24px。
- 主内容左栏约 38%，右栏约 62%；右栏内部卡片纵向排列。
- 快捷操作区域宽约左栏，单项按钮 48–52px 高，2 列等宽，间距 10–12px。
- 交易表格使用固定、稳定的列：日期 / 类型 / 账户 / 状态 / 金额。数值全部右对齐，日期和说明保持左对齐。

## 4. 色彩系统

| Token | 色值 | 用途 |
| --- | --- | --- |
| `--ink-strong` | `#15152E` | 页面标题、主金额、重要操作文本 |
| `--ink` | `#323248` | 正文、表格主要内容 |
| `--ink-muted` | `#9392A2` | 次要说明、占位符、日期 |
| `--line` | `#E7E6EE` | 卡片、分隔线、输入框边线 |
| `--surface` | `#FFFFFF` | 主内容与卡片背景 |
| `--canvas` | `#F8F8FB` | 页面背景、弱化区域 |
| `--primary` | `#4E63E9` | 主按钮、选中图标、确认状态 |
| `--primary-hover` | `#4055D8` | 主按钮悬停 |
| `--primary-soft` | `#EEF0FF` | 选中导航、浅色标签背景 |
| `--violet` | `#9656EA` | 图表辅助色、Hero 装饰 |
| `--lavender` | `#C07AEF` | 图表辅助色 |
| `--success` | `#4BAF8C` | 成功金额与成功标签文字 |
| `--success-soft` | `#E3F5ED` | 成功标签背景 |
| `--warning-soft` | `#E9EEFF` | Pending 等浅蓝状态背景 |
| `--blocked-soft` | `#F1F0F4` | 禁用/Blocked 状态背景 |

Hero 背景使用柔和的横向渐变：左侧 `#C9D6FF`，中部 `#C7BDF1`，右侧 `#E9CBE1`。禁止用高饱和、强对比渐变；保持明亮、雾面、低对比。

## 5. 字体与数字

- 推荐字体：**Inter**、Manrope 或其他现代无衬线字体；中文可回退至 `PingFang SC` / `Noto Sans SC`。
- 字重以 400、500、600、700 为主，避免极细字重。
- 页面标题/账户标题：20–22px / 700。
- Hero 主金额：34–38px / 700，字距略收紧（`-0.03em`）。
- 卡片标题和主交易名称：15–16px / 600–700。
- 正文、操作按钮：14–15px / 500–600。
- 辅助文本和表头：12–13px / 500；行高约 1.4–1.5。
- 金额使用等宽数字（`font-variant-numeric: tabular-nums`）；货币整数部分加粗，小数部分略小或保持同字号但降低字重。负数仍用深灰，正向入账用绿色。

## 6. 组件规范

### 6.1 侧栏导航

- 顶部放置深色方形品牌图标，尺寸约 28px，外部留白 20–24px。
- 导航项是 40px 高的方形图标按钮，图标尺寸 18–20px，默认 `#777686`。
- 当前页：浅蓝紫色圆角底板（约 32px 宽、高），图标为 `--primary`。
- 顶部导航与底部支持、设置、头像之间使用自动撑开空间。头像为 28px 圆形。
- 图标必须配备 tooltip 和无障碍名称；不要只依赖颜色表达当前状态。

### 6.2 Hero 与顶栏

- Hero 高度约 300–340px，内边距 42–48px。
- 左上角放“返回所有账户”的文字链接，配左箭头；其下 52–60px 为账户标题和主余额。
- 右上角为搜索框（约 230 × 42px）和“Transfer Money”主按钮（约 154 × 42px）。搜索框为半透明白色，图标置右。
- Hero 的抽象丝带/玻璃 3D 装饰位于中间偏右，以低不透明度铺陈；文字和按钮处于更高层级。没有图像资源时，用模糊的蓝紫粉渐变曲线/半透明椭圆替代，不能以照片替代。

### 6.3 KPI 指标卡

- 位于 Hero 底部，横向排列 5 张，间距 12–16px。
- 前四张展示指标（Income、Expenses、EBITDA、Revenue），第五张为“Top Spendings / Operations”摘要。
- 卡片约 185 × 70px，圆角 14–16px，背景为 `rgba(255,255,255,.62)`，带 backdrop blur；文字保持深色。
- 指标标题 12px，金额 17–19px / 700；右侧放紧凑的百分比小胶囊。避免用红绿来承载所有涨跌含义；在必要时附加箭头或文案。

### 6.4 按钮、输入与快捷操作

- **主按钮**：`--primary` 实底、白字、圆角 6–8px、水平内边距 16–18px；含 16px 图标时图标在左。
- **次级按钮**：白底或透明底、`--line` 边框、`--primary` 文字；用于 Edit limits 等低优先级动作。
- **快捷操作**：浅蓝灰背景 `#F4F6FF`，左图标为蓝色，右侧使用 16px chevron；hover 时背景稍深且整体可点击。
- **输入框**：42px 高，圆角 6–8px；占位文本为 `--ink-muted`，聚焦时使用 2px、低透明度的蓝色 focus ring。

### 6.5 Spending Limit 卡片

- 横向排版：左侧为标题、69% 大数字、到期说明和 Edit limits；右侧为半圆/甜甜圈图。
- 圆环粗度约 18–22px，主体分段采用蓝、紫、浅紫，剩余部分用 `#EEEEF6`。
- 圆心显示“Available”和可用金额；下方排列小圆点图例与百分比。图例用文字而非仅色彩区分。

### 6.6 Deposited Checks 卡片

- 顶部标题在左，左右圆形箭头按钮在右，用于切换支票；按钮不可用时降低对比度。
- 内容分左右两块：左为 Pending 徽标、来源账户、醒目金额及 Validate check 按钮；右为浅蓝灰色支票票据预览。
- 票据预览应包括品牌、付款人、收款方、Memo 和底部账号占位，不要呈现真实个人信息或完整银行账号。

### 6.7 交易表格与状态

- 标题栏左侧“Transactions”，右侧“See all”文字链接。
- 行高 34–38px，表头可省略但列对齐必须清晰；用极浅分隔线或留白分行，避免重网格。
- 状态以小号圆角标签展示：Confirm 为蓝底白字且可点； Blocked 为灰底灰字；Success 为浅绿底绿字。
- 金额右对齐：支出以 `-$327.00` 深灰显示；收入以 `+$1,260.00` 绿色显示。不要单独用颜色表达正负，必须保留 `+/-` 符号。

## 7. 交互与状态

- 所有可点击元素提供 hover、focus-visible、active 和 disabled 状态；键盘焦点清晰可见。
- 搜索框支持即时筛选或提交搜索，Esc 清除焦点；空状态需说明“未找到交易”并提供清除筛选操作。
- “Confirm”应触发确认流程（弹窗或二次确认），提交后显示加载状态，成功后更新为 Success，失败时给出可恢复的错误提示。
- 卡片 hover 只允许轻微边框/背景变化，不能整卡大幅上浮，维持金融产品的稳定感。
- 图表需提供可访问文本摘要，例如“Operations 占已用额度的 54%”。

## 8. 无障碍与内容约束

- 正文与背景对比度至少满足 WCAG AA；Hero 上的深色文本和按钮需在渐变的全部区域保持可读。
- 不要把浅灰文字用于关键金额、交易状态或主要操作。
- 图标按钮必须包含 `aria-label`；状态标签应带有文字；图表应有标题、图例和等效数据描述。
- 金额、日期和账户号均使用格式化示例数据；对敏感信息仅展示掩码，如 `Checking *1234`。
- 文案简洁、动词明确：`Transfer Money`、`Edit limits`、`Validate check`、`See all`。

## 9. AI 生成提示词

> 生成一个 1440px 宽的现代企业金融后台账户总览页。左侧为 72px 白色图标导航栏，右侧是白色内容容器。顶部 Hero 使用柔和的蓝、紫、粉渐变和抽象半透明 3D 丝带装饰；左侧显示账户余额，右侧有搜索框和蓝色 Transfer Money 按钮。Hero 底部放 5 张半透明 KPI 卡片。下方采用左窄右宽双栏：左侧有 2×2 快捷转账操作和 Deposited Checks 卡片，右侧有 Spending Limit 甜甜圈图和 Transactions 表格。使用 Inter 字体、深蓝黑文字、蓝紫主色、24px 大圆角、细灰边框和大量留白。界面应精致、轻盈、高信任感，不使用厚重阴影、深色主题、渐变文字或高饱和霓虹色。

## 10. 验收清单

- [ ] 账户余额是页面中最醒目的文本，主操作按钮在首屏右上清晰可见。
- [ ] Hero 装饰不遮挡文字、搜索框、按钮或 KPI 数据。
- [ ] 所有卡片共享一致的圆角、边框、间距和字体层级。
- [ ] 交易金额、日期、状态在各行严格对齐，正负金额语义明确。
- [ ] 图表、状态和导航在不依赖颜色时仍然可理解。
- [ ] 1024px 以下主内容能单栏阅读，关键操作仍可触达。
